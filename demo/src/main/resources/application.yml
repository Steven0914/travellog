spring:
  h2:
    console:
      enabled: true
  datasource:
    # 인메모리 데이터베이스
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: root
  jpa:
    hibernate:
      #ddl-auto: create
      ddl-auto: update
    show-sql: true
